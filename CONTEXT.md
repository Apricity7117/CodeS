# CodeS

CodeS 是面向本地或局域网浏览器使用 Codex 的交互界面。这里的语言约定用于区分用户可见的运行选择、模型来源和实际模型连接边界。

## Language

**模型目录**:
CodeS 中面向用户展示的可选模型集合，包括显示名、可选思考强度和默认思考强度。它描述界面允许选择什么，不定义模型如何连接。
_Avoid_: provider 配置, endpoint 配置, 模型提供方配置

**模型目录配置**:
CodeS 服务实例共享的机器级界面配置，用来调整模型目录的展示和思考强度选项。它不属于浏览器个人偏好，也不写入 Codex 的 `config.toml`。
_Avoid_: Codex 配置, 浏览器偏好, provider 配置

**模型目录结构校验**:
保存模型目录配置时执行的字段和取值校验。它不向模型提供方发起真实模型请求，也不证明模型一定可用。
_Avoid_: 可用性校验, 试运行模型

**模型目录配置文本**:
用户在 CodeS 设置中编辑的 JSON 文本，用来表达模型目录配置。JSON 是配置的人机编辑格式，不引入额外 YAML 依赖。
_Avoid_: YAML 配置, Codex TOML 配置

**有效模型目录**:
CodeS 实际展示给用户选择的模型目录。它由 Codex 自动发现的模型、provider 发现的模型和模型目录配置合并得到。
_Avoid_: 原始模型列表, 手动模型列表

**模型目录预览**:
设置中展示有效模型目录的只读视图。它帮助用户确认模型目录配置和自动发现结果合并后的最终模型显示、顺序、来源、隐藏状态和思考强度。
_Avoid_: 配置源码, 模型下拉

**模型选项**:
有效模型目录中的单个模型条目，包含模型 ID、模型显示名、是否可选、思考强度集合、默认思考强度和来源。界面显示模型显示名，发送请求时使用模型 ID。
_Avoid_: 模型名字符串

**模型目录顺序**:
有效模型目录中的展示顺序。默认保留自动发现顺序并追加用户新增模型；用户配置显式顺序时，显式顺序优先，未列出的模型按发现顺序补在后面。
_Avoid_: 字母排序, provider 排序

**有效模型目录失败保护**:
模型目录配置解析或保存失败时，CodeS 继续使用最后一次有效模型目录；启动时无法读取有效配置则回退到自动发现模型。
_Avoid_: 清空模型列表, 阻断聊天

**模型显示名**:
模型在 CodeS 界面中展示给用户看的名称。未配置显示名时，界面使用模型 ID 作为显示名。
_Avoid_: 模型 ID, provider 模型名

**模型提供方**:
负责实际接收模型请求并返回模型结果的服务或账号配置边界。它决定模型名称是否真实可用，以及请求使用哪种认证和协议。
_Avoid_: 模型目录, 模型下拉列表

**线程模型选择**:
用户为某个对话线程选择的模型。它表达该线程接下来使用哪个模型，不等同于全局默认模型。
_Avoid_: 默认模型, provider 选择

**隐藏模型选择**:
线程曾经选择但当前模型目录配置隐藏的模型。CodeS 保留该选择用于解释历史上下文，但发送新消息前要求用户改选可用模型。
_Avoid_: 自动切换模型, 删除历史选择

**思考强度选择**:
用户为模型请求选择的推理强度。切换模型时，只有当前思考强度不适用于新模型，才自动切到该模型的默认思考强度。
_Avoid_: 模型能力, 默认模型

**思考强度集合**:
模型允许用户选择的推理强度集合。CodeS 支持 `none`、`minimal`、`low`、`medium`、`high`、`xhigh` 六档；未配置模型沿用当前默认展示的四档 `low`、`medium`、`high`、`xhigh`。
_Avoid_: 固定思考强度菜单

**线程运行配置**:
线程上下文中保存的一组运行选择，至少包含线程模型选择和思考强度选择。不同线程的运行配置互不影响。
_Avoid_: 全局设置, 默认模型

**默认模型**:
Codex 在没有线程级选择时使用的全局模型偏好。改变默认模型应是用户显式确认的设置动作，而不是选择某个线程模型时的副作用。
_Avoid_: 线程模型选择, 临时模型选择
